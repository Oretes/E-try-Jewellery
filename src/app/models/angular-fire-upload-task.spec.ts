import { AngularFireUploadTask } from './angular-fire-upload-task';

describe('AngularFireUploadTask', () => {
  it('should create an instance', () => {
    expect(new AngularFireUploadTask()).toBeTruthy();
  });
});
